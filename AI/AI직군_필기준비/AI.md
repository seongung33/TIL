# 인공지능 정리 노트
## 딥러닝 
1. 모델선정 - 은닉층의 개수가 많을수록 성능이 좋아지지만 과적합이 발생할 확률이 높음
2. 모델컴파일 - 활성화함수, 손실함수, 옵티마이저 선택
3. 모델훈련 - 파라미터와 하이퍼파라미터의 최적값 찾기(하이퍼 파라미터는 사람이 조정)

### 지도학습

**합성곱 신경망(Convolutional Neural Network, CNN)** - 이미지 분류, 인식, 분할  
말 그래도 이미지 데이터를 유사한 것들끼리 분리함
**순환 신경망(Recurrent Neural network, RNN)** - 주로 시계열에서 사용   
역전파 과정에서 기울기 소실 문제 발견 - LSTM으로 해결 (망각, 입력, 출력 게이트가 따로 존재하여 해결)

### 비지도학습 
**워드 임베딩**과 **군집**이 있다  
- 워드 임베딩 - 단어를 벡터로 표현
  - 방식으로는 워드투벡터, 글로브가 있다
- 군집 - 아무런 정보가 없는 상태에서 데이터를 분류 
  - 머신러닝의 군집과 같다.   
### 전이학습
전이학습 - 사전에 학습이 완료된 모델을 가지고 미세 조정 기법을 이용해 학습   
사전 학습 모델 - 분석하려는 주제에 대해 많은 학습이 되어있는 모델을 이용

| 구분 | 유형 | 알고리즘 |
|------|------|----------|
| 지도 학습 (Supervised Learning) | 이미지 분류 | CNN, AlexNet, ResNet |
| 지도 학습 (Supervised Learning) | 시계열 데이터 분석 | RNN, LSTM |
| 비지도 학습 (Unsupervised Learning) | 군집 (Clustering) | Gaussian Mixture Model (GMM), Self-Organizing Map (SOM) |
| 비지도 학습 (Unsupervised Learning) | 차원 축소 | AutoEncoder, PCA |
| 전이 학습 (Transfer Learning) | 사전 학습 모델 | ELMo |
| 전이 학습 (Transfer Learning) | 전이 학습 | BERT, MobileNetV2 |
| 강화 학습 (Reinforcement Learning) | - | Markov Decision Process (MDP) |


## 머신러닝 핵심 알고리즘

### 분류 - 이산형
- K - 최근접 이웃 (K-nearest neighbor)
  - 데이터가 들어왔을 때 가장 가까운 k개의 데이터를 한 그룹으로 판단
- 서포트 벡터 머신(Support Vector Machine, SVM) - 분류를 위한 기준선 정의
  - 결정 경계를 통해 분류
  - 결정 경계는 데이터가 분류된 클래스에서 가장 멀리 떨어져 있을 때 성능이 제일 좋다
  - 마진 - 결정 경계와 가까이 붙어있는 데이터. 이 데이터들이 결정 경계를 정의하는 역할을 한다고 볼 수 있다.
  - 최적의 결정 경계는 마진을 최대로 해야한다 - 이상치를 잘 다루어야 한다.
- 결정 트리 - 이상치가 많을 때 사용
  - 여러번의 분류를 하며 내려감
  - 불확실성을 계산 - 엔트로피, 지니계수
  - 엔트로피 - 엔트로피가 높을수록 불확실성이 높음
  - 지니계수 - 불순도를 측정하는 지표, 데이터의 통계적 분산 정도를 정량화해서 표현
- 로지스틱 회귀
- 비선형문제 분류하는법
  - 비선형 커널 도입 - 다항식 커널, 가우시안 RBF 커널

#### 판단
**혼동행렬**
| 실제 \ 예측 | Positive (예측) | Negative (예측) | 지표 |
|-------------|-----------------|-----------------|------|
| Positive (실제) | TP | FN | Recall = TP / (TP + FN) |
| Negative (실제) | FP | TN | Precision = TP / (TP + FP) |
| 전체 |  |  | Accuracy = (TP + TN) / (TP + TN + FP + FN) |

- Accuracy = (TP + TN) / (TP + TN + FP + FN)
- Precision = TP / (TP + FP)
- Recall = TP / (TP + FN)
- F1-score = 2 × (Precision × Recall) / (Precision + Recall)

### 회귀 - 연속형
- 선형회귀





## 딥러닝
목적에 따라 CNN, RNN, RBM, DBM으로 분류
- CNN - 이미지 처리 우수
- RNN - 시계열 데이터(음악, 영상)에 우수
### 과적합 해결방안 - 드롭아웃
- 드롭아웃: 임의로 일부 노드를 학습에서 제외  

### 기울기 소실 문제 - 배치, 확률적 경사 하강법  
- 배치 경사 하강법: 전체 데이터셋에 대한 오류를 구한 후 기울기를 한 번만 계산하여 적용
- **확률적 경사 하강법(SGD)**: 임의로 선택한 데이터에 대해 기울기를 계산
- **미니 배치 경사 하강법**: 여러개로 나누고 미니 배치 한개마다 기울기를 구해서 평균 기울기를 이용해 업데이트 

### 옵티마이저
파라미터 변경 폭이 불안정해 학습 속도와 운동량을 조정
- 아다그라드: 많이 변하는 학습률은 작게, 적게 변하는 학습률은 크게
  - G 함수 도입: 학습이 적게 되면 크게 학습, 많이 했으면 작게 학습 
- 아다델타: G 함수로 인해 학습이 멈추는 문제를 해결하기 위해 등장
- 알엠에스프롭
  - G 값이 무한히 커지는 것을 방지: 감마를 도입하여 사용자가 조정가능

운동량을 조절하는법 
- **모멘텀(Momentum)**: SGD와 같이 사용
  - 이전 수정방향이 +, - 인지 판단하여 일정 비율만 수정

- **아담(Adam)**
  - 모멘텀과 알엠에스프롭의 장점을 결합한 경사 하강법


## CNN, 합성곱 신경망 구조
1. 입력층 - 높이, 너비, 채널(RGB)로 (높이, 너비, 채널(컬러면 3))의 구조를 가지고 있음
2. 합성곱층 - 컨불루션, 이미지 특성을 감지하기 위해 커널과 필터를 사용 - 각 영역의 특성을 강조하여 값 출력
3. 풀링층 - 특성 맵의 차원을 다운 샘플링하여 연산량을 감소 시키고 주요한 특성 벡터만 추출하여 학습을 효과적으로 할 수 있게 함
4. 완전연결층 - 3차원 벡터에서 1차원 벡터로 펼친다. 
5. 출력층 - softmax 활성화 함수를 통해 입력받은 값을 0~1 사이로 출력하여 판단



## RNN, 순환 신경망
이전 은닉층이 현재 은닉층의 입력이 되면서 반복되는 순환 구조  
기존 네트워크와의 차이점은 기억을 갖는다.    
대표적 사용처 - 자연어 처리, 영상 등의 시계열 데이터
RNN의 큰 문제 - 기울기 소멸 문제 - 이를 해결하기 위해서 LSTM 도입



## 성능 최적화
1. 데이터
2. 알고리즘
3. 앙상블
4. 하드웨어
5. GPU 이용
6. 하이퍼 파라미터, 배치 정규화, 드롭아웃

## 자연어 처리
자연어 처리시 토큰을 이용한다. 
토큰화 - 텍스트를 문장이나 단어로 분리하는 것 
불용어 - a, the, she, he 같은 분석과 관계가 없는 단어를 제거한다.
어간 추출 - 과거형 동명사 같은 형태를 기본형태로 변환한다. 


## 오토인코더 - 디코더 



## 트랜스포머 




1. 그래디언트 폭발 및 소실문제 완화를 위한 기법 - He초기화, 활성화함수로 ReLU 채택, 미니배치를 통한 배치 정규화

2.버블:  GPU 분산 학습시 발생하는 GPU 유휴시간을 지칭하는 용어