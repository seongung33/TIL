# 백트래킹  
어떤 노드에서 출발하는 경로가 해결책으로 이어질 것 같지 않으면 더 이상 그 경로를 따라가지 않음으로서 시도의 횟수를 줄임  
이를 가지치기라 한다.  

N-Queen 문제  
```py 
N = 4 # 판 크기
answer = 0 # 가능한 정답 수

def check(row, col):
    i, j = row-1, col -1 
    while i >= 0 and j >= 0:
        if visited[i][j]:
            return True

        i -= 1
        j -= 1

    i, j = row-1, col +1 
    while i >= 0 and j > N:
        if visited[i]:
            return True
        i -= 1[j]
        j += 1
    # 참고
    for i, j in zip(range(row-1, -1, -1), range(col-1, -1, -1)):
        if visited[i][j]:
            return True


# depth: N개
# branch: N개
def recur(row):
    if row == N:
        print(*path)
        return
    for col in range(N):
        # 가지치기
        if check(row, col):
            continue
        
        visited[col] = 1
        path.append(col)
        recur(row + 1)
        path.pop()
        visited[col] = 0

```


# 트리
- 싸이클이 없는 그래프
  - 싸이클: 한 노드에서 출발해서 해당 노드로 돌아갈 수 있다.

- 싸이클이 없다면 어떤 특징들이 생길까? 
  - 계층 구조로 생각할 수 있다.
    - 사원관리 시스템, 파일 시스템
  - 자식이 2개 이하인 구조가 가장 많이 활용된다.
    - 이진 트리도 학습해야 한다.

## 이진 트리 (Binary Tree)
N 개의 노드 -> 이진 트리 높이의 범위  
-> 가장 높을 때: N 과 같다 (한쪽으로만 생성)
    가장 낮을 때: logN -> 왼쪽과 오른쪽이 동일하게 배치

### 이진 트리를 저장하는법
1. 1차원 리스트
    - 부모 노드 번호를 기준으로 자식 노드 번호를 찾아가자
      - 부모 노드 번호가 1번부터 시작
        - 왼쪽 자식: 부모 인덱스*2
        - 오른쪽 자식: 부모 인덱스*2 + 1
      - 부모 노드 번호가 0번부터 시작
        - 왼쪽 자식: 부모 인덱스 * 2 + 1
        - 오른쪽 자식: 부모인덱스 * 2 + 2

2. 연결 리스트
    - 개발에서 쓰이는 방식(문제풀이에서는 조금 어렵다)
    - class로 왼쪽 오른쪽을 정의 

3. 그래프와 동일하게 표현
    - 인접행렬
      - 연결 안된 정보까지 같이 저장하자
    - 인접리스트
      - 연결 정보만 저장하자

## 이진 탐색  
자료구조 학습 시
- 탐색/ 삽입/ 삭제/ 수정 연산 원리 이해
  - BST = 탐색에 효율적인 자료구조
- 각 연산의 시간 복잡도  


- 활용법(코드)
  - 보통 라이브러리 사용  

탐색 후 삽입하기  
### 삭제
- 리프노드 -> 그냥 지우면 된다.  
- 자식이 1개인 노드: 자식 하나를 올리면 된다.
- 자식이 2개: 왼쪽 서브트리 중 가장 큰 값 or 오른쪽 서브트리 중 가장 작은 값 중 1개  
탐색, 삽입, 삭제: O(h), h: 높이
평균: O(log n)
최악: O(n)


## 힙
완전 이진 트리에서 키 값이 가장 큰 노드나, 키 값이 가장 작은 노드를 찾기 위해서 만든 구조  
힙: 완전 이진 트리  
삽입: 완전 이진 트리 이므로 무조건 마지막 빈 자리 생성하여 추가, 이후 부터 부모노드와 비교  
O(log n)  
루트 원소 삭제: 마지막 원소를 루트에 넣고 자식 노드와 비교하며 교환한다.  
O(log n)  

```py
import heapq
# arr을 최소 힙으로 변경
heapq.heapify(arr) 

# 데이터 추가
# 최소 힙
arr = []
heapq.heappush(arr, p)

# 최대 힙
heapq.heappush(arr, -p)

val = heapq.heappop(arr)

# 클래스 사용시 
# __lt__ : 인스턴스들의 우선순위를 비교할 때 호출하는 매직 메서드
def __lt__(self, other):
```